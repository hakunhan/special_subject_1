import utils.get_input

#get the total sale
total_sale = float(utils.get_input.number("Enter the total sale: "))

#calculating total sale
predict = f"{total_sale*1.23}"
print("Profit will be made form the total sale is: " + predict)
